set(INCLUDE_DIRECTORIES
  ${INCLUDE_DIRECTORIES}
  ${CMAKE_CURRENT_SOURCE_DIR}
  PARENT_SCOPE
)

if(NOT ${CPU})
  set(GPU_SOURCE
    ${GPU_SOURCE}
    ${CMAKE_CURRENT_SOURCE_DIR}/DeviceToHost.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/HostToDevice.cpp
  )
endif()

set(SOURCE
  ${SOURCE}
  PARENT_SCOPE
)
