set(TEST_INCLUDE_DIRECTORIES
  ${TEST_INCLUDE_DIRECTORIES}
  ${CMAKE_CURRENT_SOURCE_DIR}
  PARENT_SCOPE
)

if(NOT ${CPU})
  set(GPU_TEST_SOURCE
    ${CMAKE_CURRENT_SOURCE_DIR}/LogisticRegressionConfigurationTest.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/LogisticRegressionTest.cpp
  )
endif(NOT ${CPU})

set(TEST_SOURCE
  ${TEST_SOURCE}
  ${GPU_TEST_SOURCE}
  PARENT_SCOPE
)
